var page = require('webpage').create();
var fs = require('fs');

page.open('index.html', function() {
    setTimeout(function() {
        fs.write('./out.txt', page.plainText, 'w');
        phantom.exit();
    }, 200);
});
